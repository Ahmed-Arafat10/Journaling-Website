### Section 8:
- Make update button redirect to `TaskUpdate.php` page
- `TaskUpdate.php` page frontEnd + BackEnd Logic
- Delete button + BackEnd Logic
- Two buttons `mark as complete`/`undo` + BackEnd Logic